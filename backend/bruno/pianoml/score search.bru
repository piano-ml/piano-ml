meta {
  name: score search
  type: http
  seq: 5
}

get {
  url: https:/api.pianoml.org/score/search?fullKey
  body: none
  auth: inherit
}

params:query {
  fullKey: 
  ~keyword: 
  ~genreId: {{genreId}}
  ~gradeStart: {{gradeStart}}
  ~gradeEnd: {{gradeEnd}}
  ~offset: {{offset}}
  ~limit: {{limit}}
}

headers {
  Accept: */*
  Authorization: Bearer {{token}}
}
