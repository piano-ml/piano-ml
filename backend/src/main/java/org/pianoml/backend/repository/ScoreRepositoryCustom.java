package org.pianoml.backend.repository;

// This class was previously annotated with @Repository and provided a custom implementation.
// We now provide `ScoreRepositoryImpl` which follows Spring Data's naming convention (Repository + Impl)
// Keep this class here as a reference (not registered as a bean).
//
/*
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import jakarta.persistence.TypedQuery;
import jakarta.persistence.criteria.CriteriaBuilder;
import jakarta.persistence.criteria.CriteriaQuery;
import jakarta.persistence.criteria.Predicate;
import jakarta.persistence.criteria.Root;
import org.pianoml.backend.entity.Score;
import org.pianoml.backend.entity.User;
import org.springframework.stereotype.Repository;

import java.util.Arrays;
import java.util.List;
import java.util.UUID;

// @Repository
public class ScoreRepositoryCustom implements IScoreRepositoryCustom {

  @PersistenceContext
  private EntityManager em;

  // Original implementation moved to ScoreRepositoryImpl.java
}
*/
